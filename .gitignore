# Secrets — NEVER commit these
.env
*.env
config.local.*
secrets.*

# Python
__pycache__/
*.pyc
venv/
.venv/
tempCodeRunnerFile.py

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Database dumps (can be large / may contain real client data)
*.dump
*.backup

# Uploaded product photos — generated at runtime, NOT in git.
# In production, nginx serves this folder directly (bypassing FastAPI).
uploads/